# Execution Scripts

This folder contains deterministic Python/Node scripts for the 3-layer architecture.

## Available Scripts

- (To be created as needed per directives)

## Usage

Scripts are called by the AI orchestration layer based on directives in `directives/`.
